# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'greensyntax-foundation-module' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for greensyntax-foundation-module
  pod 'Toaster'
  pod 'NVActivityIndicatorView'

  target 'greensyntax-foundation-moduleTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
